<nav class="bg-gradient-to-r from-pink-600 via-red-700 to-blue-600 fixed top-0 left-0 right-0 shadow-xl z-50 backdrop-blur-md bg-opacity-90">
  <!-- Decorative Blobs for Navbar Background -->
  <div class="absolute top-0 left-0 w-40 h-40 bg-pink-300 opacity-30 rounded-full blur-2xl -z-10 animate-pulse"></div>
  <div class="absolute top-0 right-0 w-52 h-52 bg-blue-300 opacity-30 rounded-full blur-2xl -z-10 animate-pulse"></div>
  <div class="absolute bottom-0 left-1/2 w-28 h-28 bg-red-200 opacity-20 rounded-full blur-xl -z-10 animate-pulse"></div>
  <div class="max-w-6xl mx-auto px-6 py-3 flex justify-between items-center relative">
    <!-- Logo -->
    <a href="/" class="flex items-center space-x-2 group">
      <span class="inline-block bg-white rounded-full p-2 shadow-md">
        <svg class="w-7 h-7 text-pink-600 group-hover:scale-110 transition" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <path d="M3 10.5V21h18V10.5L12 3 3 10.5z" />
        </svg>
      </span>
      <span class="text-white font-extrabold text-2xl tracking-tight bg-clip-text bg-gradient-to-r from-white via-pink-100 to-blue-200">
        air-bnb
      </span>
    </a>
    <!-- Navigation Links -->
    <ul class="flex space-x-4 items-center">
      <% if (isLoggedIn) { %>
      <% if (user && user.usertype === 'guest') { %>
        <li>
          <a href="/homes" class="px-4 py-2 rounded-xl font-semibold text-white transition-all duration-200
            <%= currentPage === 'Home' ? 'bg-white/20 ring-2 ring-pink-200 shadow' : 'hover:bg-white/10 hover:ring-1 hover:ring-pink-100' %>">
            Home-List
          </a>
        </li>
        <li>
          <a href="/" class="px-4 py-2 rounded-xl font-semibold text-white transition-all duration-200
            <%= currentPage === 'goToHome' ? 'bg-white/20 ring-2 ring-pink-200 shadow' : 'hover:bg-white/10 hover:ring-1 hover:ring-pink-100' %>">
            Go to Home
          </a>
        </li>
        <li>
          <a href="/favourites" class="px-4 py-2 rounded-xl font-semibold text-white transition-all duration-200
            <%= currentPage === 'favourites' ? 'bg-white/20 ring-2 ring-pink-200 shadow' : 'hover:bg-white/10 hover:ring-1 hover:ring-pink-100' %>">
            Favourites
          </a>
        </li>
        <li>
          <a href="/bookings" class="px-4 py-2 rounded-xl font-semibold text-white transition-all duration-200
            <%= currentPage === 'booking' ? 'bg-white/20 ring-2 ring-pink-200 shadow' : 'hover:bg-white/10 hover:ring-1 hover:ring-pink-100' %>">
            Bookings
          </a>
        </li>
      <% } else  { %>
        <li>
          <a href="/host/add-Home" class="px-4 py-2 rounded-xl font-semibold text-white transition-all duration-200
            <%= currentPage === 'Home' ? 'bg-white/20 ring-2 ring-pink-200 shadow' : 'hover:bg-white/10 hover:ring-1 hover:ring-pink-100' %>">
            Add Home
          </a>
        </li>
        <li>
          <a href="/host/host-home-list" class="px-4 py-2 rounded-xl font-semibold text-white transition-all duration-200
            <%= currentPage === 'hostHomes' ? 'bg-white/20 ring-2 ring-pink-200 shadow' : 'hover:bg-white/10 hover:ring-1 hover:ring-pink-100' %>">
            Host Homes
          </a>
        </li>
          <% } %>
      <% } %>
      <% if (!isLoggedIn) { %>
        <li>
          <a href="/Signup" class="px-4 py-2 rounded-xl font-semibold text-white transition-all duration-200
            <%= currentPage === 'Signup' ? 'bg-white/20 ring-2 ring-pink-200 shadow' : 'hover:bg-white/10 hover:ring-1 hover:ring-pink-100' %>">
            Signup
          </a>
        </li>
        <li>
          <a href="/login" class="px-4 py-2 rounded-xl font-semibold text-white transition-all duration-200
            <%= currentPage === 'login' ? 'bg-white/20 ring-2 ring-pink-200 shadow' : 'hover:bg-white/10 hover:ring-1 hover:ring-pink-100' %>">
            Login
          </a>
        </li>
      <% } else { %>
        <li>
          <form action="/logout" method="POST">
            <button type="submit" class="px-4 py-2 rounded-xl font-semibold text-white bg-gradient-to-r from-pink-500 to-red-500 hover:from-pink-600 hover:to-red-600 shadow transition-all duration-200">
              Logout
            </button>
          </form>
        </li>
      <% } %>
    </ul>
  </div>
</nav>