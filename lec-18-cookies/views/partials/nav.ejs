<nav class="bg-red-700 fixed top-0 left-0 right-0 shadow-md z-50">
  <div class="max-w-6xl mx-auto px-6 py-3 flex justify-between items-center">
    <ul class="flex space-x-6 items-center">

      <!-- Logo/Home -->
      <li>
        <a href="/" class="text-white font-semibold px-3 py-2 rounded
          <%= currentPage === 'index' ? 'bg-red-900' : 'hover:bg-red-600' %>">
          air-bnb
        </a>
      </li>

      <% if (isLoggedIn) { %>
        <li>
          <a href="/host/add-Home" class="text-white font-semibold px-3 py-2 rounded
            <%= currentPage === 'Home' ? 'bg-red-900' : 'hover:bg-red-600' %>">
            Home
          </a>
        </li>
        <li>
          <a href="/host/host-home-list" class="text-white font-semibold px-3 py-2 rounded
            <%= currentPage === 'hostHomes' ? 'bg-red-900' : 'hover:bg-red-600' %>">
            Host Homes
          </a>
        </li>
        <li>
          <a href="/homes" class="text-white font-semibold px-3 py-2 rounded
            <%= currentPage === 'homeList' ? 'bg-red-900' : 'hover:bg-red-600' %>">
            Home-List
          </a>
        </li>
        <li>
          <a href="/" class="text-white font-semibold px-3 py-2 rounded
            <%= currentPage === 'goToHome' ? 'bg-red-900' : 'hover:bg-red-600' %>">
            Go to Home
          </a>
        </li>
        <li>
          <a href="/favourites" class="text-white font-semibold px-3 py-2 rounded
            <%= currentPage === 'favourites' ? 'bg-red-900' : 'hover:bg-red-600' %>">
            Favourites
          </a>
        </li>
        <li>
          <a href="/bookings" class="text-white font-semibold px-3 py-2 rounded
            <%= currentPage === 'booking' ? 'bg-red-900' : 'hover:bg-red-600' %>">
            Bookings
          </a>
        </li>
      <% } %>

      <!-- Login / Logout -->
      <li>
        <% if (!isLoggedIn) { %>
          <a href="/login" class="text-white font-semibold px-3 py-2 rounded
            <%= currentPage === 'login' ? 'bg-red-900' : 'hover:bg-red-600' %>">
            Login
          </a>
        <% } else { %>
          <form action="/logout" method="POST">
            <button type="submit" class="text-white font-semibold px-3 py-2 rounded hover:bg-red-600">
              Logout
            </button>
          </form>
        <% } %>
      </li>

    </ul>
  </div>
</nav>
